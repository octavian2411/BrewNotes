# BrewNotes
Simple app to store coffee recipes

## Structure
- `frontend/`: Angular app (SSR)
- `backend/`: Node/Express API

## Development
- Frontend dev server: `npm start` (from repo root)
- Frontend tests: `npm test` (from repo root)
- Backend server: `npm.cmd --prefix backend start`
